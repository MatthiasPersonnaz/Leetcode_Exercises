#include <iostream>
#include <iostream>
#include <cassert>
#include <array>
#include <cmath>
#include <vector>
#include <tuple>

using namespace std;

struct ListNode {
    int val;
    ListNode *next;
    ListNode() : val(0), next(nullptr) {}
    ListNode(int x) : val(x), next(nullptr) {}
    ListNode(int x, ListNode *next) : val(x), next(next) {}
};

int main() {
    return 0;
    ListNode l = ListNode([[1,4,5],[1,3,4],[2,6]];
}